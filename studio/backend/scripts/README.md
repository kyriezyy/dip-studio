# Scripts

## run_dev.py

启动开发服务器，支持热重载。

```bash
python scripts/run_dev.py
```

## init_db.py

初始化数据库表结构和初始数据。

```bash
python scripts/init_db.py
```

运行前请确保:
1. MariaDB 服务已启动
2. 已配置正确的数据库连接信息（通过环境变量或 .env 文件）
